= form_for [announcement, announcement.comments.new] do |f|
  = f.label :author, "#{current_user.username}: "
  = f.text_field :content, class: 'form-control content_lenght comment_field'
  = f.hidden_field :user_id, value: current_user.id
  = f.submit 'Dodaj!', class: 'btn btn-primary'